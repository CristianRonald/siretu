server:
  port: 8093
eureka:
  instance:
    hostname: localhost
  client: 
    service-url:
      defaultZone: http://eureka-server:8761/eureka
spring:
  application:
    name: nlp
  config:
    import: optional:configserver:http://localhost:8888
